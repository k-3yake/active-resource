# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /:resource/:id               controllers.ActiveResourceController.get(resource:String, id: Int)
PUT     /:resource/:params            controllers.ActiveResourceController.put(resource:String, params: String)



